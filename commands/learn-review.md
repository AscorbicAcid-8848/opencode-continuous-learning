---
description: 立即复盘当前会话，把稳定事实或可复用流程持久化
---

现在复盘当前会话中已经完成并验证的工作。不要重新执行任务，也不要把一次性的项目状态、临时错误、提交号、令牌、密钥或未经验证的猜测保存下来。

如果发现稳定的用户偏好，使用 `learning_memory` 的 `user` 目标；跨项目都适用的机器环境事实或长期约定使用 `memory`；只属于当前项目的架构、命令、约束或持久决策使用 `project`。如果发现可复用的技术流程、排错路径、修复方法或用户明确纠正过的工作方式，先用 `learning_skill` 查找已有 Skill，再更新或创建。不要把项目事实复制到全局 Memory；没有值得长期保存的内容时，明确说明无需写入，不要为了完成命令而制造知识。
